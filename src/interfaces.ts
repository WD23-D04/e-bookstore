export interface IBook {
    title : string;
    author : string;
    price : number;
    category : string;
}

export interface IPerson {
    firstName : string;
}

/* export interface IWorker {

} */